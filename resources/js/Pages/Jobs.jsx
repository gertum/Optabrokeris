import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import {Head, Link} from '@inertiajs/react';

export default function Jobx({ auth, laravelVersion, phpVersion }) {
    return (
        <>
            <Head title="Jobs" />

            <div>
                TODO jobs
            </div>
        </>
    );
}